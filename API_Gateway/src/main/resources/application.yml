server:
  port: 8090
  
spring:    
  cloud:
    gateway:
      routes:
      - id: AUTHOR-SERVICE
        uri:
          lb://AUTHOR-SERVICE
        predicates:
        - Path=/author/**
        
      - id: ITEM-SERVICE
        uri:
          lb://ITEM-SERVICE
        predicates:
        - Path=/item/**
              
  application:
    name: API_GATEWAY
